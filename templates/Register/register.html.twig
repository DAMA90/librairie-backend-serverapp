{% extends 'base.html.twig' %}

{% block title %}Inscription - Bibliothèque{% endblock %}

{% block body %}
<div class="row justify-content-center">
    <div class="col-md-6">
        <div class="card shadow-lg p-4 mt-5">
            <h2 class="text-center mb-3">📝 Créer un compte</h2>

            <!-- Messages de succès -->
            {% for message in app.flashes('success') %}
                <div class="alert alert-success">{{ message }}</div>
            {% endfor %}

            <!-- Formulaire d'inscription -->
            {{ form_start(form) }}
                <div class="mb-3">
                    {{ form_label(form.nom, 'Nom complet') }}
                    {{ form_widget(form.nom, {'attr': {'class': 'form-control'}}) }}
                </div>

                <div class="mb-3">
                    {{ form_label(form.email, 'Adresse Email') }}
                    {{ form_widget(form.email, {'attr': {'class': 'form-control'}}) }}
                </div>

              <div class="mb-3">
     {{ form_label(form.password, 'Mot de passe') }}
    {{ form_widget(form.password, {'attr': {'class': 'form-control', 'minlength': '12', 'maxlength': '12', 'required': 'required'}}) }}
    <small class="text-muted">Le mot de passe doit contenir exactement 12 caractères.</small>
    </div>

            {{ form_end(form) }}

            <!-- Lien vers la connexion -->
            <div class="text-center mt-3">
                <p>Déjà un compte ? <a href="{{ path('app_login') }}" class="text-primary">Se connecter</a></p>
            </div>
        </div>
    </div>
</div>
{% endblock %}
